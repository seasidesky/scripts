#!/bin/sh

LANGUAGE=en sudo netstat -punta 
echo "--------------------------------------------------------------"

if [ "$1" ] && [ $1 = '-a' ] ; then
    sudo lsof -i4 -n -P
    echo "--------------------------------------------------------------"
fi
